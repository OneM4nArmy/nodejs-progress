const name = "unknown try n find me.";
let age = 1111;

console.log(name);

if (age === 1111) {
    console.log("do some searching :D")
} else {
    console.log("imagine ...")
}

if (age > 1110) {
    console.log(`older than ${age}`)
}